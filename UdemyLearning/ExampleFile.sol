pragma solidity >= 0.7.0 < 0.9.0;

contract ExampleFile {
    uint a = 500;
    bool spend = false;
    string notifySpend = "you have spent money";

    function AaaBbb() public view returns(uint) {
        uint b = 200;
        uint r = a + b;
        return r;
    }

}